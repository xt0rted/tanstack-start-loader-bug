# TanStack Start SSR Error

1. `npm ci`
2. `npm run dev`
3. Navigate to each link and the pages should load fine
4. Navigate to the first one and refresh so it runs through SSR, should load fine
5. Navigate to the second one and refresh so it runs through SSR, an error will be thrown
